import 'package:flutter/material.dart';

class SubscribeTab extends StatelessWidget {
  const SubscribeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Subscribe content goes here'));
  }
}